# DreamKey Automation

AI systems &amp; automation agency — automation stacks for Dubai real-estate agencies and US pet-product Shopify stores.

This repo also hosts the static services-portfolio site (`index.html`), Netlify-ready via `netlify.toml`. Live once connected to Netlify: point a new Netlify site at this repo/branch, publish directory `.`, no build command needed.
